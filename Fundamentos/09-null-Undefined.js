let valor // não será inicializada
console.log(valor);  // aqui temos o valor undefined, pois o valor foi inicializado.

valor = null ; //null significada que a variável não está apontando para nenhum endereço de memória.
// ela não tem um valor dentro dela.
// null = ausência de valor
console.log(valor); //diferente do undefined, a variável foi inicializada, 
//porém não aponta pra nenhum lugar de memória.